# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/finance_tracker
spring.datasource.username=postgres
spring.datasource.password=Parva*123
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# JWT Configuration
jwt.secret=mySecretKeyForJWTTokenGenerationMustBeAtLeast256BitsLongForHS256Algorithm
jwt.expiration=86400000

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.parva.financetracker=DEBUG
```

**IMPORTANT:** Replace `YOUR_PASSWORD_HERE` with your actual PostgreSQL password!

**Save the file**

---

## ✅ Verify Your Security Structure

Your folders should now have:
```
src/main/java/com/parva/financetracker/
├── exception/
│   ├── GlobalExceptionHandler.java
│   ├── ResourceAlreadyExistsException.java
│   └── ResourceNotFoundException.java
├── security/
│   ├── CustomUserDetailsService.java
│   ├── JwtAuthenticationFilter.java
│   └── JwtUtil.java
└── config/
    └── SecurityConfig.java



# Hibernate Enum Fix
spring.jpa.properties.hibernate.type.preferred_jdbc_type_code_for_enum=VARCHAR